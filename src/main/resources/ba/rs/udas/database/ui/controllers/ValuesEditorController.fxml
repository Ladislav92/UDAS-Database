<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ListView?>
<?import javafx.scene.control.Separator?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.VBox?>
<BorderPane xmlns:fx="http://javafx.com/fxml"
  fx:controller="ba.rs.udas.database.ui.controllers.ValuesEditorController" fx:id="rootPane">

  <left>
    <ListView prefWidth="200"></ListView>
  </left>
  <center>
    <ListView prefWidth="200"></ListView>
  </center>
  <right>
    <VBox spacing="10">
      <Button text="Add"/>
      <Button text="Edit"/>
      <Separator/>
      <Button text="Remove"/>
    </VBox>
  </right>

  <!--  <fx:define>
      <Integer fx:id="notificationHeight" fx:value="50"/>
    </fx:define>

    <top>
      <Pane minHeight="$notificationHeight" minWidth="${rootPane.width}" fx:id="notificationAreaPane"/>
      <NotificationPane content="${notificationAreaPane}" minHeight="$notificationHeight" fx:id="notificationPane"/>
    </top>

    <center>
      <VBox alignment="CENTER" maxWidth="180" spacing="10">
        <BorderPane.margin>
          <Insets top="-50"/>
        </BorderPane.margin>

        <VBox alignment="CENTER">
          <Label styleClass="login-logo, login-logo__top" text="UDAS">
            <padding>
              <Insets bottom="-10"/>
            </padding>
          </Label>
          <Label text="──────────────────────"/>
          <Label styleClass="login-logo, login-logo__bottom" text="%databaseLabel_text"/>
        </VBox>

        &lt;!&ndash;temporary text values&ndash;&gt;
        <TextField promptText="%loginField_promptText" fx:id="loginField"/>
        <PasswordField promptText="%passwordField_promptText" fx:id="passwordField"/>

        <HBox alignment="CENTER_LEFT">
          <Button GridPane.rowIndex="3" onAction="#onLoginButtonClicked" text="%loginButton_text" fx:id="loginButton"/>
        </HBox>
      </VBox>
    </center>

    <bottom>
      <HBox alignment="CENTER_RIGHT">
        <BorderPane.margin>
          <Insets bottom="20" right="25"/>
        </BorderPane.margin>

        <ComboBox fx:id="languageComboBox" prefWidth="100" onAction="#onLanguageComboBoxAction"/>
      </HBox>
    </bottom>-->
</BorderPane>
